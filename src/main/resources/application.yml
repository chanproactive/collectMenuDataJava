server:
  port: 9000
  servlet:
    contextPath: /api

spring:
  jpa:
    hibernate:
      ddl-auto: create

  datasource:
    url: jdbc:mysql://localhost:3306/db_webservice
    username: webservice
    password: P@ssw0rd

  security:
